{
  "name": "browser-adapter",
  "version": "1.0.0",
  "description": "HTTP service that uses Playwright to fetch rendered HTML for the C++ crawler.",
  "main": "browserAdapter.js",
  "scripts": {
    "start": "node browserAdapter.js"
  },
  "dependencies": {
    "express": "^4.22.2",
    "playwright": "^1.45.0"
  }
}